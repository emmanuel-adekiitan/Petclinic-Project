name: "Terraform IaC & DevSecOps Pipeline"

on:
  push:
    branches: [ main, "feature/*" ]
  pull_request:
    branches: [ main ]

# Global permissions fallback
permissions:
  id-token: write
  contents: read

jobs:
  checkov-iac-scan:
    name: "Checkov IaC Security Scan"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Checkov Security Scan
        uses: bridgecrewio/checkov-action@master
        with:
          framework: terraform
          output_format: cli
          soft_fail: false
          skip_check: CKV2_AWS_11

  terraform-ci:
    name: "Terraform Lint & Plan"
    needs: checkov-iac-scan
    runs-on: ubuntu-latest
    # Explicitly enforce OIDC token minting permissions at the job level
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: ./Terraform-Petclinic
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::128793514564:role/AllowGithub
          aws-region: ca-central-1

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate -no-color

      - name: Terraform Plan
        run: terraform plan -no-color -input=false
        env:
          TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}